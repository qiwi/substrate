export { forEach as lodashEach, map as lodashMap, set as lodashSet, get as lodashGet } from 'lodash';
export { each as underscoreEach, map as underscoreMap } from 'underscore';
export { Promise as BluebirdPromise } from 'bluebird';

export {
  IStringMap,
  IAnyMap,
  IEventEmitter,
  ILoggerMethod,
  ILogger,
  TLogLevel,
  TUtilGet,
  TUtilSet,
  TUtilEach,
  TUtilMap,
  ICloneable,
  ICurrency,
  IPromise,
  IPromiseConstructor,
  IStorage,
  IMoney
} from '../main';
